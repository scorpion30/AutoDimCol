<Window x:Class="AutoDimCol.ColumnsWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoDimCol"
             mc:Ignorable="d" 
             Width="300" Height="150">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Button Name="selectButton" Click="selectButton_Click" Width="90" Height="25" Background="Green" Foreground="White" Content="Select Columns"></Button>
        <StackPanel VerticalAlignment="Center" Grid.Column="1" Orientation="Horizontal">
            <TextBlock Text="Columns No" ></TextBlock>
            <TextBox IsEnabled="False" Name="columnsNo" Width="70" Margin="10 0 0 0"/>
        </StackPanel>
        <Button Name="apply" Click="apply_Click" Grid.Column="1" Grid.Row="1" Width="80" Height="25" Background="LightGray" Content="Apply">
            
        </Button>
        
    </Grid>
</Window>
